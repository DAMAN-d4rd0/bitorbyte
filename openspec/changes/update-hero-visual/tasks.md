## 1. HTML Structural Updates

- [ ] 1.1 Remove the placeholder `div` from the hero section.
- [ ] 1.2 Implement the new image container with a specific class for the background image.
- [ ] 1.3 Add the overlay `div` at the bottom of the image container.
- [ ] 1.4 Insert the value proposition text within the overlay.

## 2. Visual Styling & CSS

- [ ] 2.1 Create the CSS rule for the hero image container using `background-image: url('assets/people.jpeg')` and `background-size: cover`.
- [ ] 2.2 Implement the semi-transparent dark gradient overlay at the bottom of the container.
- [ ] 2.3 Style the overlay text (color, alignment, typography) to ensure maximum readability.
- [ ] 2.4 Adjust the container's height and proportions for visual balance.

## 3. Verification & Polishing

- [ ] 3.1 Verify the image loads correctly and covers the container without distortion.
- [ ] 3.2 Check text readability against the image on different screen sizes.
- [ ] 3.3 Ensure the hero layout remains responsive and doesn't break the page flow.
