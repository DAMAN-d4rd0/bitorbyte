## 1. HTML Refactoring

- [x] 1.1 Identify and remove the redundant wrapper `div` in the hero visual column.
- [x] 1.2 Consolidate image and overlay into a single container `div`.
- [x] 1.3 Move the `assets/people.jpeg` background style to this main container.
- [x] 1.4 Update the overlay `div` to be a direct child of the main container.

## 2. Styling & Centering

- [x] 2.1 Apply `text-align: center` to the overlay content.
- [x] 2.2 Set the container width to `100%` and verify it spans the full available grid width.
- [x] 2.3 Adjust the overlay positioning to ensure it is perfectly centered and responsive.
- [x] 2.4 Verify the bottom gradient remains effective for text readability.

## 3. Verification

- [x] 3.1 Confirm the HTML structure is flattened (one div for the image + overlay).
- [x] 3.2 Verify the text is centered horizontally on both desktop and mobile.
- [x] 3.3 Ensure the image still covers the area correctly without distortion.
